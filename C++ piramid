#include <iostream>
using namespace std;

int main() {

    for ( int baris = 1 ; baris <= 10; baris++ ) {
        for (int kolom = 1 ; kolom <= baris ; kolom++) {
            cout << "*";
        }
        cout << endl;
    }

}
